import datetime as dt

def add_one(x):
    """Adds one to input integer
        input: input
        output: input + 1
    """

    return x + 1

def sub_one(x):
    """Subtracts one from the input integer
        input: input
        output: input - 1
    """

    return x - 1
